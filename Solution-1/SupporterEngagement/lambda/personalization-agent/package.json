{
  "name": "personalization-agent",
  "version": "1.0.0",
  "description": "Personalization Agent Lambda function for Supporter Engagement Platform",
  "main": "index.js",
  "scripts": {
    "build": "tsc && cp -r ../shared dist/ && cp -r ../intent-detection dist/ && cp -r ../context-management dist/ && cp -r node_modules dist/",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.709.0",
    "@aws-sdk/client-dynamodb": "^3.709.0",
    "@aws-sdk/lib-dynamodb": "^3.709.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}
